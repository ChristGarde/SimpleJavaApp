language: java
notifications:
  email: false
before_deploy:
  provider: script
  script: ./jenkins/scripts/deliver.sh
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
  on:
    branch: master
